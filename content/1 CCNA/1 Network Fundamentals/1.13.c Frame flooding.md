tags: #topic

Refere-se ao processo de enviar o frame recebido para todas as interfaces ativas com exceção da interface que o quadro foi recebido.

O Frame Flooding ocorre quando o switch não possui o endereço MAC de destino indicado no frame em sua tabela MAC.

Quando o host responde o switch aprende seu endereço MAC parando assim com o flood.